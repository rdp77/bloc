interface:
  display_name: "Bloc"
  short_description: "Repo-specific patterns and workflows for bloc"
  default_prompt: "Use the bloc repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true